services:
  postgres:
    image: postgres:16

    environment:
      POSTGRES_DB: my-market-app-db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin

    ports:
      - "5432:5432"

    volumes:
      - postgres_dev_data:/var/lib/postgresql/data

volumes:
  postgres_dev_data: